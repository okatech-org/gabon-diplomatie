/**
 * Shared types re-exported from Convex constants.
 * These are the core enums and types used across all apps.
 */

// Re-export from convex constants for convenience
export type {
  Doc,
  Id,
} from "../../convex/_generated/dataModel";
